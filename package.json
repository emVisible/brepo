{
  "name": "briefrepo-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "BriefRepo — 新员工的项目导航仪，本地优先的入职加速器",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/emVisible/brepo"
  },
  "scripts": {
    "dev": "pnpm --filter @briefrepo/webui dev",
    "build": "turbo run build",
    "test": "vitest run",
    "bench": "node packages/analyzer-core/bench-analyze.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.base.json || true"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.11",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.11"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
